# 2026-06-22 V7.8 聊天状态板资产基线

## 本次基线

本次将桃桃最新 PRD、原型源码、状态板截图和验收脚本整理为独立 GitHub 资产仓库，用于后续指导 App、小程序和服务端开发。

## 当前版本

- 状态板版本：V7.8
- 生产流程：10 条
- 手机状态：100 个
- 覆盖矩阵：7 层
- 顶部胶囊优先级：6 级
- 截图证据：106 张

## 本次关键产品口径

### 登录

- 所有 App 聊天前必须先手机号登录。
- 验证码错误使用轻提示。
- 上传照片、3D 桃桃生成、命名和邀请不进入注册阻断。

### 邀请另一半

- 用户登录后可以先使用桃桃。
- 未绑定状态下，顶部胶囊显示邀请另一半。
- 点击顶部胶囊后出现邀请半屏。
- 邀请半屏明确分成微信分享和手机号搜索两条路径。
- 微信发送成功后不新增聊天气泡，只让顶部胶囊变成等待。
- 手机号绑定提示发送成功后不新增聊天气泡，只让顶部胶囊变成等待。
- 对方必须确认绑定，才进入三人聊天。

### 聊天

- 桃桃是第三个参与者，不是系统提醒器。
- AI 能力先由桃桃气泡承接，再在桃桃消息下方附着小胶囊。
- 顶部胶囊是索引和找回机制，不是聊天内容。
- 半屏只由用户主动点击触发。
- 已删除重复/误导状态：新建小事不做草稿箱；保存后的找回归入顶部综合抽屉；单项胶囊抽屉、对方端无新建痕迹等画板不再作为研发需求。

## 当前验证

在 `prototype/` 下已通过：

```bash
npm run build
npm run test:e2e:v8
```

已生成当前截图：

```bash
npm run screenshots:v8
```

## 已知技术提示

- `npm install` 后 `npm audit` 报 1 个 high severity 依赖风险，本次没有执行 `npm audit fix --force`，避免破坏原型锁文件。
- Vite 构建提示 chunk 超过 500 kB，这是原型阶段可接受的提醒，不影响当前状态板验收。

## 下一步

1. 继续审查小窝记忆和今天的小事抽屉。
2. 将邀请绑定、顶部胶囊和小事对象拆成接口契约。
3. 按 App 与小程序双端拆开发任务。
4. 将 agent 触发策略从 UI 状态板沉淀为服务端/agent 层规则。
